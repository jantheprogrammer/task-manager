import React from 'react'

export const NoTasks = () => {
  return <div className="tasks loading">You have no tasks</div>
}

export const Loading = () => {
  return <div className="tasks loading">Loading...</div>
}
